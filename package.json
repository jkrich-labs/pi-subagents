{
  "name": "pi-subagents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Background subagent fleet for pi",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test tests/*.test.ts",
    "verify": "make verify",
    "benchmark:subagents": "node harness/benchmark/run.ts"
  },
  "pi": {
    "extensions": [
      "./extensions/subagents"
    ]
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@earendil-works/pi-tui": "0.84.2",
    "typebox": "1.3.7",
    "typescript": "^5.9.2"
  }
}
